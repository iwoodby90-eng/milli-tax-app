# Visual Capture Workflow (PR #66 gate)

The GitHub token available to me cannot write files under `.github/workflows/` (GitHub returns 404 for workflow-file changes without the `workflow` scope). This note documents the intended file so it can be committed by a token with workflow scope.

Target path: `.github/workflows/visual-capture.yml` on branch `ci/visual-capture-pr66` (created from `fix/nav-ai-documents-stabilization` @ f878c58).

See the PR description for the full YAML.
