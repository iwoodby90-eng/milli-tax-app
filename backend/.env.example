ENVIRONMENT=sandbox
DATABASE_URL=postgresql://user:password@localhost:5432/milli

APPLE_CLIENT_ID=com.milli.taxvault
AUTH_JWT_SECRET=
AUTH_ACCESS_TOKEN_MINUTES=15
AUTH_REFRESH_TOKEN_DAYS=30

# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
PROVIDER_TOKEN_ENCRYPTION_KEY=

PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
PLAID_WEBHOOK_URL=https://<your-render-service>.onrender.com/plaid/webhook
PLAID_REDIRECT_URI=

# Optional anti-abuse app signal; NOT user identity.
CLIENT_API_KEY=
